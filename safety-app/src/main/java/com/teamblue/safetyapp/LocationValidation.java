package com.teamblue.safetyapp;

public class LocationValidation {

public static void  getLocation() {
		
mileLimit =;// max amount of distance to be able to call LSUPD to your location

if getLocation > mileLimit{
    system.out.println("Call 911")
}
else{
    markLocation;// marks location of current user
    currentLocation = markLocation;
}
	}
return currentLocation;
}
